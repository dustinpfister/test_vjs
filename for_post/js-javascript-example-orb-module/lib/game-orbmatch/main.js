(function () {

    var canvasObj = canvasMod.createCanvas({
        container: document.getElementById('canvas-app')
    });
	
	console.log(canvasObj);

    //var orb = orbMod.createFromLevel([1, 0, 2, 0], 3);
    //console.log(orb);
}
    ());
