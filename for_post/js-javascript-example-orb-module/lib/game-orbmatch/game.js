(function (api) {
    // create and return a new game object
    api.create = function (opt) {
        opt = opt || {};
        var game = {};
        return game;
    };
}
    (this['GameMod'] = {}));
